plugins {
    id 'com.android.application'
}

android {
    namespace 'com.bubble.autoplayer'
    compileSdk 35

    defaultConfig {
        applicationId 'com.bubble.autoplayer'
        minSdk 30
        targetSdk 35
        versionCode 1
        versionName '1.0'
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}
